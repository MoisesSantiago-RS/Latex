\documentclass{article}
\usepackage[spanish]{babel}
\decimalpoint
\usepackage{comment} %Comentarios
\usepackage{listings} %Codigo
\usepackage[dvipsnames]{xcolor} %Paquete de colores en Latex
\usepackage{amsmath} 
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsfonts} %Simbolos exclusivos, como los simbolos de campos
\usepackage{tikz} %Sirve para dibujar muchas cosas en general
%\usetikzlibrary{graphs,graphdrawing} %paquetería que tal vez no es necesaria para poder dibujar gráficas
%\usetikzlibrary{positioning} %Definir nodos y distancias más precisas
\usepackage{blindtext} %Parrafos de texto
\usepackage{multicol} %Columnas

\usetikzlibrary{shapes.geometric}


\title{Algunos resultados de K-tupla dominación}
\author{Moisés S. S.}
\date{\today}



\begin{document}
	%Comenzamos "formalmente" la escritura del texto desde el principio.
	\maketitle
	\section*{Introduction}

	%investigar como hacer citas correctamente
Se analizan y se muestran resultados sobre $k$-tupla dominación provenientes del artículo \textit{ro220017} y de las referencias de los mismos. Los resultados posteriores buscan mostrar nuevas o mejorar viejas cotas ante el numero de $k$-tupla dominación usando diferentes parametros y cuantificadores para dichas cotas. También se busca la profundización y explicación explicita de las  demostraciones presentadas en el artículo, así como aclaraciones de convenciones asumidas en las referencias y la adaptación de las mismas a la dominación utilizada en el artículo.

	%Se necesita hacer la escritura de cada definición necesaria y agregar todas las que se necesiten o eliminar las redundantes.
	\subsection{Algunos resultados básicos necesarios}

\textbf{Def. 1} Def. de gráfica \newline
\textbf{Def. 2} Def. de vecindades cerradas y abiertas\newline
\textbf{Def. 3} Def. de grados abiertos y grados cerrados.\newline
	%Añadiremos la notación de grado de un vértice sobre un conjunto o en caso de ser necesario, se anexará como definición
\textbf{Def. 4} Def. de grado máximo y mínimo.\newline
\textbf{Def. 5} Def. de subgráfica.\newline
\textbf{Def. 6} Gráfica libre de garras.\newline
\textbf{Def. 7} Def. de dominación.\newline
\textbf{Def. 8} Def. de número de dominación.\newline
\textbf{Def. 9} Def. de $k$-dominación.\newline
\textbf{Def. 10} Def. de número de $k$-dominación.\newline
\textbf{Def. 11} Def. de $l$-total $k$- dominación.\newline
	%K-tupla dominación es un caso particular de la definición anterior
\textbf{Def. 12} Def. de $k$-tupla dominación.\newline
	%Hacemos nota e incapie sobre que valorees puede tomar k para que las definiciones sean equivalentes.
\textbf{Def. 13} Def. numero de $k$-tupla dominación.\newline

	$$FinDeDefiniciones$$

	%Comenzamos la escritura de los primeros resultados y su uso, además de hacerlo en un solo sentido
\section{Resultados relacionados con otros numeros de dominación.}

Comenzamos el estudio del número de $K$-tupla dominación respecto a cotas superiores. Para este capitulo, nos enfocaremos en buscar nuevas cotas superiores en términos de dos enteros $k$ y $k'$, además de utilizar los numeros de dominación y $k$-dominación para mejorar nuestro acotamiento, así como mostrar ejemplos puntuales donde las cotas son justas para alguna gráfica particular o alguna familia de gráficas.\\

	%Falta revisar a profundidad este resultado y su demostración.
La demostración del siguiente teorema se hace en una publicación anterior del mismo autor. Este resultado era un problema abierto y recopilado en el año 2020, (referencia 6). Este resultado principalmente fue planteado para $k=2$ Posteriormente se demostró dos años depués para cualquier $k\geq 2$, además de dar una mejora considerable a la cota original.\\

\textbf{Problema} Dar una cota superior para $\gamma_{xk}(G)$ en términos de $\gamma_k(G)$ para cualquier gráfica $G$ con $\delta(G)\geq k-1$.\\

El problema tiene una solución relativamente simple, dada la relación de la $k$-dominación y la $k$-tupla dominación, tenemos la siguiente desigualdad:\\

$$\gamma_{xk}(G)\leq k\gamma_k(G)$$

Probemos que esta desigualdad se cumple.\\
Sea $D$ un $\gamma_k$-conjunto y definimos al conjunto $D'\subseteq V(G)$ tal que $D\subseteq D'$ y $\delta_{D'}(x) \geq k-1$ para toda $x\in D$. Como $D$ es un conjunto $k$-dominante, $\forall x \in (V(G)\setminus D)$, $\delta_{D'}(x)\geq k$ y por la definición del conjunto $D'$, $\forall v\in D$, $\delta_{D'}(v)\geq k-1$, por lo que, $D'$ es un conjunto $k$-tupla dominante de $G$.\\


Por último, por la construcción del conjunto $D'$, a lo más, se agregaron $k-1$ vértices más al conjunto $D$ para cada vértice del mismo, es decir, a lo más se agregaron $(k-1)|D|$ vértices, por lo que, $|D'|=|D|+|D'\setminus D|$. Entonces:

\begin{align*}
\gamma_{xk}(G)&\leq |D'|\\
&\leq |D|+|D'\setminus D|\\
&\leq |D|+(k-1)|D|\\
\Longrightarrow \gamma_{xk}(G)&\leq k\gamma_k(G)
\end{align*}


Ya que se resolvió el problema anterior y se otorgó una justificación a la misma, posterioremente podemos hacer un refinamiento de la desigualdad anteior, haciendolo mejorar en al menos 1. El refinamiento mencionado se cita y se demuestra en el siguiente teorema.\\

\textbf{Teorema 2.1} Sea $k \geq 2$ entero. Para cualquier gráfica $G$ con $\delta(G) \geq k-1$.

$$\gamma_{xk}(G) \leq k\gamma_{k}(G)-(k-1)^2$$

\textbf{Dem.}\\

Sea $D$ un $\gamma_k(G)$-conjunto. Sabemos que $\gamma_{xk}(G)\leq |V(G)|$ y además, tenemos que considerar dos casos. El primer caso es cuando $k|D|-(k-1)^2\ > |V(G)|$, lo que termina la prueba en este caso. El segundo caso  es cuando $k|D|-(k-1)^2\leq |V(G)|$, que será el caso qeu supondremos para el resto de la demostración.\\

Como $D$ es un conjunto $k$-dominante, sabemos que $|D|\geq k$, de lo que podemos deducir los iguiente:\\

%Se demuestra la existencia del conjunto U
\begin{align*}
|D|&\geq k\\
\Longrightarrow |D|-1 &\geq k-1\\
\Longrightarrow (k-1)(|D|-1) &\geq (k-1)^2\\
\Longrightarrow (k-1)|D|-(k-1) &\geq (k-1)^2\\
\Longrightarrow (k-1)|D|-(k-1)^2 &\geq k-1\\
\Longrightarrow k|D|-(k-1)^2 -|D|&\geq k-1\\
\Longrightarrow V(G)-|D|&\geq k|D|-(k-1)^2 -|D|\\
\Longrightarrow V(G)-|D|&\geq k-1
\end{align*}
\newline
Ahora, como vimos anteriormente, podemos encontrar al menos $k-1$ vértices fuera del conjunto, por lo que podemos definir los siguientes conjuntos. Sean $U=\{u_1, ..., u_{k-1}\}\subset V(G)\setminus D$, $D'=D\cup U$ y $D_0=\{v\in D : \delta_{D'}(v)<k-1\}$. \\
Una vez definidos los conjuntos anteriores, podemos deducir las siguientes desigualdades, las cuales provienen del conteo de aristas, la primera entre los conjuntos $U$ y $D_0$ y la segunda desigualdad de los conjuntos $U$ y $D\setminus D_0$.\\

\begin{figure}

\begin{center}
\begin{tikzpicture}[scale=1.2]
    % --- DIAGRAMA SUPERIOR ---
    % Elipses principales D y U
    \draw (0,0) ellipse (1cm and 1.8cm) node[above=2.1cm] {$D$};
    \draw (3.5,0) ellipse (0.8cm and 1.8cm) node[above=2.1cm] {$U$};

    %Nodos en U
    \filldraw (3.3,0.85) circle (2pt) node (p8) {};
    \filldraw (3.6,0.85) node (p81) {$u_1$};

    %Nodos de densidad
    %\filldraw (3.3,0.25) circle (0.3pt) node (p9) {};
    %\filldraw (3.3,0.05) circle (0.3pt) node (p10) {};
    %\filldraw (3.3,-0.15) circle (0.3pt) node (p11) {};


    \filldraw (3.3,-0.65) circle (2pt) node (p12) [above=0.55cm]{$\vdots$};
    \filldraw (3.7,-0.65) node (p121) {$u_{k-1}$};


    
    %	Nodos en D
    \filldraw (0.2,1.2) circle (2pt) node (p7) {};

    %Nodos de densidad (pequeños)
   % \filldraw (0,1.2) circle (0.3pt) node (p6) {};
    %\filldraw (-0.15,1.2) circle (0.3pt) node (p5) {};
    %\filldraw (-0.3,1.2) circle (0.3pt) node (p4) {};
    
    \filldraw (-0.5,1.2) circle (2pt) node (p3) [right]{$\; \dots$};


    % Subconjunto D_0 dentro de D
    \draw (0,-0.6) ellipse (0.6cm and 1cm) node[above=0.5cm, left=0.1cm] {$D_0$};
    
    % Punto dentro de D_0
    \filldraw (0.2,-0.5) circle (2pt) node (p1) {};
    
    % aristas azules (D_0 - U)
    \draw[Aquamarine] (p1) -- (3.2, 0.8);
    \draw[Aquamarine] (p1) -- (3.2, 0);
    \draw[Aquamarine] (p1) -- (3.2, -0.6);

    % aristas azules (D - D_0)
    \draw[BurntOrange] (p1) -- (0.2, 1);
    \draw[BurntOrange] (p1) -- (0, 1);
    \draw[BurntOrange] (p1) -- (-0.2, 1);
    \draw[BurntOrange] (p1) -- (-0.4, 1);

    % aristas verdes (D - U)
    \draw[LimeGreen] (p7) -- (3.2, 0.8);
    \draw[LimeGreen] (p7) -- (3.2, 0);
    \draw[LimeGreen] (p7) -- (3.2, -0.6);
    

    % --- SEPARACIÓN ---
   % \begin{scope}[yshift=-5.5cm]
    
    % --- DIAGRAMA INFERIOR ---
    % Elipses principales X e Y
    %\draw (0,0) ellipse (1cm and 1.8cm) node[above=1.8cm] {$X$};
    %\draw (3.5,0) ellipse (1cm and 1.8cm) node[above=1.8cm] {$Y$};
    
    % Subconjunto N(S) dentro de Y
    %\draw (3.5,-0.5) ellipse (0.6cm and 0.8cm) node[above=0.4cm] {$N(S)$};
    
    % Punto dentro de N(S)
    %\filldraw (3.3,-0.7) circle (2pt) node (p2) {};
    
    % Líneas azules (aristas)
    %\draw[blue!70!black] (p2) -- (0.5, 0.6);
    %\draw[blue!70!black] (p2) -- (0.7, -0.1);
    %\draw[blue!70!black] (p2) -- (0.5, -0.8);
    
    %\end{scope}
\end{tikzpicture}
\end{center}

\caption{Conjuntos de vértices definidos en la gráfica $G$ y las posibles aristas entre los mismos}
    \label{fig:grafica_1}
\end{figure}


$$\sum_{v\in D_0}\delta_{D'}(v)\geq \sum_{i=1}^{k-1}\delta_{D_0}(u_i) \hspace{2.5cm} (1)$$
\newline

El lado izquierdo de la desigualdad, son todas las aristas posibles entre los vértices del conjunto $D_0$ y el conjunto $D'$, apoyandonos de la Figura 1, son las aristas verdes y las aristas azules. El lado derecho de la desigualdad, son todas las posibles aristas entre los vértices de $U$ y los vértices de $D_0$, una vez más, apoyandonos de la Figura 1, son las aristas de color azul. Además, sabemos que $U\subseteq D'$, lo que justifica la desigualdad\\


$$|D\setminus D_0|(k-1)\geq \sum_{i=1}^{k-1}\delta_{D\setminus D_0}(u_i) \hspace{2cm} (2)$$


Sabemos que $\forall x \in D\setminus D_0$, tiene $\delta_{D'}(x)\geq k$, por lo que, cada vértice en $D\setminus D'$ puede ser tener como vecinos a todos los vértices del conjunto $U$, es decir, pueden haber a lo más $|D\setminus D_0|(k-1)$ aristas posibles. Lo anterior representa el lado izquierdo de la desigualdad y en la Figura 1, son las aristas de color verde. El lado derecho de la desigualdad son la suma de los grados de todos los vértices del conjunto $U$ sobre el conjunto $D\setminus D_0$, el cual queda acotado por $|D\setminus D_0|(k-1)$.\\

Sumando las desiguadades $(1)$ y $(2)$, tenemos que:\\

\begin{align*}
\sum_{v\in D_0}\delta_{D'}(v)+|D\setminus D_0|(k-1)&\geq \sum_{i=1}^{k-1}\delta_{D_0}(u_i)+\sum_{i=1}^{k-1}\delta_{D\setminus D_0}(u_i)\\
& \geq \sum_{i=1}^{k-1}\delta_{D}(u_i)\\
\Longrightarrow \sum_{v\in D_0}\delta_{D'}(v)+|D\setminus D_0|(k-1)&\geq k(k-1)\\
\end{align*}

Como el conjunto $D$ ya es $k$-dominante de $G$, para cualquier vértice $u_i\in U$ debe tener al menos $k$ vecinos dentro de $D$ y como hay $k-1$ vértices en $U$, la suma total de aristas entre $D$ y $U$ debe de ser al menos $k(k-1)$.\\


Despejamos a $\sum_{v\in D_0}\delta_{D'}(v)$ de la desigualdad

\begin{align*}
\Longrightarrow \sum_{v\in D_0}\delta_{D'}(v)\geq& |D\setminus D_0|(k-1) + k(k-1)\\
\text{Multiplicamos por (-) e invertimos la desigualdad}\\
\Longrightarrow -\sum_{v\in D_0}\delta_{D'}(v)\leq& |D\setminus D_0|(k-1)-k(k-1) \hspace{1cm} (3)
\end{align*}
%\newline


Ahora, definimos al conjunto $D''\subseteq V(G)$ como un conjunto de cardinalidad mínima entre todos los super conjuntos $W$ de $D'$ tales que $\delta_W(x)\geq k-1$ para todo $x \in D$. Con la definición del conjunto $D''$, es fácil ver que, para todo vértice $x$ dentro del conjunto $D'\setminus D_0$ tiene $\delta_{D'}(x)\geq k-1$, por lo que, $x \in D''$. Solo resta ver que sucede con lo vértices en $D_0$.\\

Sea $v\in D_0$. Sabemos que todos los vértices $v$ en $D$ tienen $\delta_W(v)\geq k-1$, particularmente, si $v\in D_0$, entonces, el vértice $v$ tiene al menos $k-1-\delta_{D'}(v)$ vecinos en $(W\setminus D')\subseteq (W\setminus D)$, como esto es para cada vértice de $D_0$, tenemos que a lo más son: $|D_0|(k-1)-\sum_{v\in D_0}\delta_{D'}(v)$ vecinos totales. (Falta hacer el dibujo y además hacer la aclaración escrita de como se ven estas aristas). \\
De la misma forma, observamos que al contar los vecinos de cada $v\in D_0$, estos están en $D''\setminus D'$, por lo que, al sumar todos estos vecinos, son a lo menos $|D''\setminus D'|$ ya que $D''$ es de cardinalidad mínima.\\

De lo anterior, tenemos la siguiente desigualdad:\\

\begin{align*}
|D''\setminus D'|&\leq |D_0|(k-1)-\sum_{v\in D_0}\delta_{D'}(v)\\
\text{Utilizando la desigualdad (3):}\\
\Longrightarrow|D''\setminus D'|&\leq |D_0|(k-1)+|D\setminus D_0|(k-1)-k(k-1)\\
\Longrightarrow|D''\setminus D'|&\leq (|D_0|+|D\setminus D_0|)(k-1)-k(k-1)\\
\Longrightarrow|D''\setminus D'|&\leq (|D|)(k-1)-k(k-1) \hspace{1.5cm} (4)\\
\end{align*}

Por la definición del conjunto $D''$, para cada $x\in D$, $\delta_{D''}(x)\geq k-1$ y, como $D$ es $\gamma_k(G)$-dominante y $D\subset D''$, para cualquier $y\in V(G)\setminus D$, $\delta_{D''}\geq k$, entonces, esto es equivalente a, para todo vértice $x\in V(G)$, $\delta_{D''}(v)\geq k-1$, por lo tanto, $D''$ es un conjunto $k$-tupla domiante de $G$ y $\gamma_{xk}(G)\geq |D''|$. \\

Por último, recordemos las cardinalidades de $D''$ $D'$ y posteriormente sus tituiremos estos valores sobre la desigualdad (4), $|D''|=|D'|+|D''\setminus D'|$ y $|D'|=|D|+k-1$, entonces: $|D''\setminus D'|=|D''|-|D'|$\\


\begin{align*}
|D''|-|D'|&\leq (|D|)(k-1)-k(k-1)\\
\Longrightarrow |D''|&\leq |D'|+|D|(k-1)-k(k-1)\\
\Longrightarrow|D''|&\leq|D|+k-1+ |D|(k-1)-k(k-1)\\
\Longrightarrow|D''|&\leq |D|+k-1+k|D|-|D|-k^2+k\\
\Longrightarrow|D''|&\leq k|D|-k^2+2k-1\\
\Longrightarrow|D''|&\leq k|D|-(k-1)^2\\
\end{align*}

Como $D$ es $\gamma_k(G)$-dominante y $D''$ es $k$-tupla dominante, podemos concluir que:\\
$$\gamma_{xk}(G)\leq k\gamma_k(G)-(k-1)^2 $$  \newline
Lo que termina la prueba. $\hspace{7.5cm}\qed$\\
%$\blacksquare$


El siguiente resultado muestra una nueva cota superior para el número de $k$-tupla dominación de una gráfica $G$, en términos de $k$-tupla dominación y $k'$-tupla dominación con $k \in \{1, ..., k-1\}$.\\

\textbf{Teorema 2.2} Sea $k, k'$ dos enteros tales que $k > k' \geq 1$,
$$\gamma_{xk}(G) \leq \gamma_{xk'}(G) + (k - k')\gamma_{k}(G)$$
\textbf{Dem.}\\
Sea $S$ un $\gamma_{xk'}(G)$-conjunto y sea $D$ un $\gamma_{k}(G)$-conjunto. Notemos lo siguiente:\\
\begin{center}
$\forall v \in V(G), \delta_{S}[v] \geq k'$
$\forall u \in V(G)\setminus D, \delta_{D}(u) \geq k$
\end{center}
Sea $D_{0} = \{v \in D : \delta_{D \cup S}(v) < k-1\}$.\\
	%D_{0} \subset D
Veamos porque el conjunto anterior puede existir.\newline
\begin{align*}
		v \in D_{0} \setminus S & \Longrightarrow \delta_{S}(v) \geq k'\\
		& \Longrightarrow \delta_{S \cup D} \geq k'\\
\end{align*}
Por otro lado, tenemos el siguiente caso
\begin{align*}
    	v \in D_{0} \cap S & \Longrightarrow \delta_{S}[v] \geq k'\\
    	& \Longrightarrow \delta_{S}(v) \geq k'-1\\
    	& \Longrightarrow \delta_{S \cup D}(v) \geq k'-1\\
\end{align*}

Por hip. $k > k'$, por lo que, de los casos anteriores en donde se pueden encontrar los vértices $D_{0}$, ya que consideraremos solo los vértices que cumplan $k' < k-1$ y $k' -1 < k$ respectivamente. Esto implica que:\\
$$\sum_{v \in D_{0}\setminus S} \delta_{D\cup S}(v) \geq k'|D_{0}\setminus S|,  \sum_{v \in D_{0}\cap S} \delta_{D\cup S}(v) \geq (k'-1)|D_{0} \cap S|$$
Sumando ambas desigualdades, tenemos lo siguiente:\\
$$\sum_{v\in D_{0}\setminus S} \delta_{D \cup S}(v) + \sum_{v\in D_{0} \cap S} \delta_{D \cup S}(v) \geq k'|D_{0} \setminus S| + (k'-1)|D_{0} \cap S|$$
   
\begin{align*}
    	& \Longrightarrow \sum_{v\in D_{0}} \delta_{D \cup S}(v) \geq k'(|D_{0}| - |D_{0} \cap S|) + (k'-1)(|D_{0}| + |S| - |D_{0} \cup S|)\\
    	& \Longrightarrow \sum_{v\in D_{0}} \delta_{D \cup S}(v) \geq k'|D_{0}| - k'|D_{0} \cap S| + (k'-1)|D_{0}| + (k'-1)|S| - (k'-1)|D_{0} \cup S|\\
    	& \Longrightarrow \sum_{v\in D_{0}} \delta_{D \cup S}(v) \geq k'|D_{0}| - k'|D_{0}| - k'|S| + k'|D_{0} \cup S| + (k'-1)|D_{0}| + (k'-1)|S| - (k'-1)|D_{0} \cup S|\\
    	& \Longrightarrow \sum_{v\in D_{0}} \delta_{D \cup S}(v) \geq |D_{0} \cup S| + (k'-1)|D_{0}| - |S|\\
    	& \Longrightarrow \sum_{v\in D_{0}} \delta_{D \cup S}(v) \geq (k'-1)|D_{0}| + |D_{0}\setminus S| & (2.1)
\end{align*}
Como $|D \setminus D_{0}| \geq |D \setminus (D_{0} \cup S)|$ y por hipotésis $k > k'$, tenemos que:\\
    
\begin{align*}
    & (k-1)|D \setminus D_{0}| \geq (k'-1)|D\setminus D_{0}| + |D\setminus D_{0}|\geq (k'-1)|D\setminus D_{0}| + |D\setminus (D_{0} \cup S)|  &  (2.2)
\end{align*}
    
De las desigualdades 2.1 y 2.2, sumamos y obtenemos lo siguiente:\\
    
\begin{align*}
    & \sum_{v\in D_{0}} \delta_{D \cup S}(v) + (k-1)|D \setminus D_{0}| \geq (k'-1)|D_{0}| + |D_{0}\setminus S| + (k'-1)|D\setminus D_{0}| + |D\setminus (D_{0} \cup S)|\\
    & \sum_{v\in D_{0}} \delta_{D \cup S}(v) + (k-1)|D \setminus D_{0}| \geq (k'-1)(|D_{0}| + |D\setminus D_{0}|) + |D_{0}\setminus S| + |D \setminus (D_{0}\cup S)|\\
    & \sum_{v\in D_{0}} \delta_{D \cup S}(v) + (k-1)|D \setminus D_{0}| \geq (k'-1)|D| + |D\setminus S| & (2.3)
\end{align*}
    %Esto último se cumple ya que por hip. $\delta(G) \geq k-1$, por lo que, podemos considerar todos los vértices que puedan cumplir esta caracteristica.
Definimos el conjunto $W' \subset V(G)$ un conjunto de cardinalidad mínima entre todos los superconjuntos $W \subset D\cup S$ tal que $\delta_{W}(v) \geq  k-1, \forall v \in D$, por la hipotésis de $\delta(G)\geq k-1$. Por definición del conjunto $D_{0}$, implica que $\forall x \in D\setminus D_{0}$, $\delta_{D\cup S}(x) \geq k-1$, de esta forma podemos deducir que, $\forall v \in D_{0}$, tienen al menos $k-1-\delta_{D\cup S}$ vecinos en el conjunto $W\setminus (D\cup S)$. De la minimalidad de W' tenemos que:\\

$$|W'\setminus (D\cup S)| \leq |D_{0}|(k-1) - \sum_{v\in D_{0}} \delta_{D\cup S}(v)$$

Utilizando la desigualdad (2.3):\\
\begin{align*}
    \sum_{v\in D_{0}}\delta_{D\cup S}(v) \geq (k'-1)|D| + |D\setminus S| - (k-1)|D\setminus D_{0}|\\
\end{align*}
Entonces,
\begin{align*}
    |W'\setminus (D\cup S)| & \leq (k-1)|D_{0}| - (k'-1)|D| - |D\setminus S| + (k-1)|D\setminus D_{0}|\\
    & =(k-1)|D| - (k'-1)|D| - |D\setminus S|\\
    & = (k - k')|D| - |D \setminus S|
\end{align*}

    %Se verifica que W es conjunto k-tupla dominante
Solo queda verificar que $W'$ es un conjunto $k$-tupla dominante. \\
Si $v\in D \subset W'$, por la construcción del conjunto $W'$ tenemos que $\delta_{W'}(v) \geq k-1$, por lo que $\delta_{W'}[v]\geq k$\\
Si $v\notin D$, como $D\subset W'$ y como $D$ es $k$-dominante, entonces, $\delta_{W}(v) \geq k$.

Por lo que $W'$ es un conjunto $k$-tupla dominante.\\

Por último notemos que:\\
    
\begin{align*}
    %Desigualdad final 
    \gamma_{xk}(G) &\leq |W'|\\
    & = |S|+ |D\setminus S| + |W'\setminus (D\cup S)|\\
    & \leq |S|+ |D\setminus S| + ((k-k')|D|-|D\setminus S|)\\
    & = |S| + (k-k')|D|\\
    & = \gamma_{xk'}(G) + (k-k')\gamma_{k}(G)
\end{align*}
    Lo que termina la prueba.\\

    La desigualdad obtenida anteriormente, podemos notar que la desigualdad es justa para algunas gráficas con una $k$ en particular. Por ejemplo, consideramos $k=k'+1=2$ $(k'=1)$, para la siguiente gráfica:\tikz \draw[rotate=30] (0,0) ellipse [x radius=6pt, y radius=3pt];\\



%Dibujo de la gráfica, dos ruedas de orden 5 con los centros conectados.
\begin{center}
\begin{tikzpicture}[scale=2.2,
    every node/.style={circle, draw, fill=white, inner sep=1.5pt}]

% --- Lado izquierdo, Rueda de orden 5 ---
\node (a1)[fill=blue!20] at (0,1) {$v_1$};
\node (a2)[fill=blue!20] at (1,1) {$v_2$};
\node (a3)[fill=blue!20] at (1,0) {$v_3$};
\node (a4)[fill=blue!20] at (0,0) {$v_4$};
\node (c1)[fill=orange!40] at (0.5,0.5) {$x$};

\draw (a1)--(a2)--(a3)--(a4)--(a1);
\draw (c1)--(a1);
\draw (c1)--(a2);
\draw (c1)--(a3);
\draw (c1)--(a4);

% --- lado derecho Rueda de orden 5 ---
\node (b1)[fill=green!20] at (3,1) {$u_1$};
\node (b2)[fill=green!20] at (4,1) {$u_2$};
\node (b3)[fill=green!20] at (4,0) {$u_3$};
\node (b4)[fill=green!20] at (3,0) {$u_4$};
\node (c2)[fill=orange!40] at (3.5,0.5) {$y$};

\draw (b1)--(b2)--(b3)--(b4)--(b1);
\draw (c2)--(b1);
\draw (c2)--(b2);
\draw (c2)--(b3);
\draw (c2)--(b4);

% --- Arista que conecta los centros ---
\draw (c1)--(c2);

\end{tikzpicture}\newline
\end{center}
%\newpage
	
	%Ejemplo directo del teorema 2.2 para el que la desigualdad es exacta
	Podemos exihibir explicitamente los conjuntos dominantes que necesitamos y podemos verificar que la desigualdad es justa para $k=2$.\\
	Sean $X=\{x,y\}$ conjunto dominante mínimo, $W=\{v_2, v_4, u_4, u_2\}$ conjunto $2$-dominante mínimo y $T=\{v_2, v_3, x, u_1, u_4, y\}$ conjunto $2$-tupla dominante mínimo. Por lo que: $\gamma(G)=2$, $\gamma_{2}(G)=4$, $\gamma_{x2}(G)=6$, por lo que:\\
	\begin{align*}
	\gamma_{x2}(G)&\leq \gamma(G) + (2-1)\gamma_{2}(G)\\
	6 &\leq  2 + 4\\
	6 & = 6
	\end{align*}
	Como consecuencia directa del teorema 2.2, tenemos el siguiente resultado para $k'=1$, lo que mejora el resultado obtenido en el teorema 2.1, siempre que $\gamma(G)<\gamma_{k}-(k-1)^2$, lo que es equivalente a $\gamma(G)+(k-1)^2<\gamma_{k}(G)$\\

	%Teo 2.2 para el caso K'=1
	\textbf{Corol. 2.3} Sea $k\geq 2$ un entero. Para cualquier gráfica $G$ con $\delta(G)\leq k-1$\\
	$$\gamma_{xk}(G) \leq (k-1)\gamma_{k}(G) + \gamma(G)$$

	Continuando con otra consecuencia directa derivada del teorema 2.2, el cual considera el caso $k'=k-1$.\\
	%consultar las referencias citadas y los resultados previamente mencionados.
	
	%teorema 2.2 para el caso k'=k-1
	\textbf{Corolario 2.4} Sea $K \geq 2$ un entero. Para cualquier gráfica $G$ con $\delta(G) \geq k-1$ \\
	$$\gamma_{xk}(G) \leq \gamma_{x(k-1)}(G)+\gamma_{k}(G)$$ 
	
	
	\begin{comment}
		Cambiamos la convención utilizada en el artículo original.
		$G_{k,r}=k_{k-1} \lor N_{r}$ \\\\
		$N_{r}$ va a ser la gráfica $(K_{r})^{c}$, la gráfica de orden $r$ sin aristas.
	\end{comment}
	
	Para el corolario anterior, tenemos una gráfica para la cual la desigualdad anterior es justa. Para cualesquiera $k,r \in \mathbb{Z}$, tal que $r\geq k \geq 2$, sea $G_{k,r}$ la gráfica definida por la suma de $(K_{k-1}, (K_{r})^{c})$, es decir, $G_{k,r}=K_{k-1}+(K_{r})^{c}$, podemos deducir lo siguiente y verificar que la desigualdad es justa para esta clase de gráficas.

\begin{align*}
\gamma_{xk}(G_{k,r}) & = |V(G_{k,r})|=k-1+r\\
\gamma_{x(k-1)}(G_{k,r}) & =k-1\\
\gamma_{k}(G_{k,r}) & =r\\
%Por \ el \ corolario \ 2.4 \ tenemos \ lo \ siguiente:\\
\Longrightarrow k-1+r & = k-1+r
\end{align*}

% Podemos mostrar un ejemplo más númerico que pueda funcionar.
	
\subsection{Cotas inferiores en términos de otros números de dominación}

	%El autor utiliza un resultado conveniente, pero no se demuestra ni se especifican las condiciones para las cuales se utiliza el resultado original aquí. El resultado usado es el siguiente: \\
Ahora, el siguiente resultado proviene de la referencia 15, $l$-total $k$-dominación, en la cual debemos definir y conocer algunos conceptos y sus posteriores resultados para poder entender de donde viene el siguiente resultado:\\
Para cualquier gráfica $G$ con $\delta(G) \geq k > k' \geq 1$ \\
$$\gamma_{xk}(G) \geq \gamma_{k'}(G)+k-k'$$
	
	
	%Veamos la desigualdad citada y vamos a "demostrar" el resultado para implementarlo aquí. 
	%Definición necesaria para la desigualdad
	
\textcolor{blue}{\textbf{Definición 2.1}} para dos enteros $l \geq 0$ y $k>0$, $X$ es un conjunto $l$-total $k$-dominante de vértices de una gráfica $G=(V,E)$, es decir, para cada vértice $v$ en $X$, $v$ tiene al menos $l$ vecinos en $X$ y para cada vértice $v$ en $V \setminus X$, $v$ tiene al menos $k$ vecinos en $X$. \\

	%Vamos a ejemplificar los casos para cada valor de l, k y el grado mínimo de G, en donde tenemos 6 posibles combinaciones.
\textcolor{blue}{\textbf{Nota.}} Si existe un conjunto $l$-total $k$-dominante, entonces $\delta(G) \geq min\{l,k\}$, por lo que existe un vértice de grado $\delta(G)$ en $X$ o en $V(G) \setminus X$. Si $\delta(G) \geq l$, entonces $l$-total $k$-dominante conjuntos existen, ya que el conjunto de vértices de $V(G)$ es uno de estos conjuntos. Por lo que, para $l \leq k$, $l$-total $k$-dominante conjunto existe si y solo si $\delta(G) \geq l$. En el caso $k \leq \delta(G) < l$, el conjunto $l$-total $k$-dominante puede no existir.\\

Veamos cada caso y como funciona cada uno de ellos.\\

	%En este caso existe y tenemos a V(G) como dicho conjunto.
\textbf{Caso 1.} $l \leq k \leq \delta(G)$\\
Es inmediato que se cumple en este caso, ya que si $\delta(G)$ es mayor a los indices $l$ y $k$, siempre existe un conjunto $l$-total $k$-dominante. El más inmediato es el conjunto $V(G)$, aunque no podemos asegurar que este último sea el más pequeño que podemos encontrar.\\

	%En este caso existe y tenemos a V(G) como dicho conjunto.
\textbf{Caso 2.} $l \leq \delta(G) \leq k$\\
Para este caso el autor de la cita nos da la existencia del conjunto $V(G)$ como el conjunto $l$-total $k$-dominante. Análogamente al caso anterior, no podemos asegurar que sea el único o el más pequeño.\\

	%En este caso solo se cumple si el grado mínimo es igual a l, en otro caso no se cumple.
\textbf{Caso 3.} $\delta(G) \leq l \leq k$\\
No son suficientes los vecinos de cada vértice para satisfacer las condiciones del conjunto.\\
Supongamos que existe el conjunto $l$-total $k$-dominante, a saber $V(G)$ y $v \in V(G)$, es el vértice de grado mínimo. Por lo que el conjunto puede existir si $l = \delta(G)$, en caso de ser menor estricto que $l$, el conjunto dominante no puede existir, pues $v$ no tiene vecinos suficientes para dominar a $l$ vértices dentro del conjunto.\\

	%Análogo al caso 1.
\textbf{Caso 4.} $k \leq l \leq \delta(G)$\\
Caso análogo al caso $1$. Tenemos a $V(G)$ como el conjunto $l$-total $k$-dominante.\\

	%Caso mencionado anteriormente donde puede o no existir.
\textbf{Caso 5.} $k \leq \delta(G) \leq l$\\
Puede existir o no existir, depende de la gráfica que tengamos, solo podemos dar ejemplos en los cuales funciona o no funciona.\\
	%Necesiatamos dibujar las "grráficas" par las cuales podemos encontrar que funciona o no funciona.

\textbf{Caso 6.} $\delta (G) \leq k \leq l$\\
Análogo al \textit{caso 3}, el conjunto solo podría existir en el caso $\delta(G) = k$, en el caso de desigualdad estricta no puede existir.\\

	%Con esto queda justificado porque no existe el conjunto, pero falta ver si se pueden caracterizar conjuntos más allá del conjunto de vértices y también hacer los dibujos correspondientes.

	
Cuando existe el conjunto $l$-total $k$-dominante, el número $l$-total $k$-dominante es la cardinalidad minima de estos conjuntos. Denotaremos el número como $\gamma_{l,k}(G)$. Con esta notación, $\gamma_{0,1}(G)$ es el número de dominación $\gamma(G)$. Por otro lado, tenemos que $\gamma_{0,k}(G)$ es el número de $k$-dominación $\gamma_{k}(G)$. $\gamma_{1,1}(G)$ es el número de dominación total y $\gamma_{k,k}(G)$ es el número de $k$-total dominación definido por \textit{Kulli}.\\
	
	%Esta última nota justificará una hipotésis necesaria para la desigualdad antes mencionada.
	
Utilizaremos la definición de $\gamma_{k,k}$ aplicada a la siguiente afirmación.\\
	
\textcolor{blue}{\textbf{Afirmación 2.3}} Si $\gamma_{l,k}(G)$ existe para gráfica $G=(V,E)$, entonces $\gamma_{l',k'}(G)$ existe para cada $l' \leq l$, $k' \leq k$, y $\gamma_{l',k'}(G) \leq \gamma_{l,k}(G) - min\{l-l', k-k'\}$.\\
	
\textbf{Dem:}\\
	%En la demostración estamos "quitando" los vértices que nos sobran para poder construir un conjunto que cumpla lo que estamos buscando a partir del conjunto \gamma_{l,k}(G).

Si $X$ es un $\gamma_{k,l}(G)$ conjunto y si $Y \subset X$ tal que $ |Y| = min\{l - l', k - k'\}$, por lo que para cada vértice $v \in X \setminus Y$, tenemos que:\\
$\delta_{X\setminus Y}(v) \geq l - l + l' = l'$,  por la cardinalidad con la que se tomó $Y$. 
De la misma forma $\delta_{V(G)\setminus(X \setminus Y)}(v) \geq k - k + k' = k'$.
Por lo que $X \setminus Y$ es un $l'$-total $k'$-dominante conjunto de $G$.\\

Ahora, vamos a utilizar la afirmación anterior adaptado a $k$-tupla dominación.\\

	%sustitución necesaria para usar la desgualdad en k-dominación.
Sean $l = k-1$ y $l'= 0$, entonces\\ 	
\begin{align*}
	\gamma_{xk}(G) &= \gamma_{k-1,k}(G)\\
	\gamma_{k'}(G) &= \gamma_{0,k'}(G)
\end{align*}

Sustituyendo lo anterior en la \textit{afirmación 2.3}, tenemos lo siguiente:\\	
	%Es un caso particular de la desigualdad original.

Sean $k'\leq k$, $0 \leq k-1$, tal que $\delta(G) \geq k \geq k'\geq 1$, entonces\\
\begin{align*}
	\gamma_{0,k'}(G) &\leq \gamma_{k-1,k}(G) - min\{k-1-0, k-k'\}\\
	\gamma_{k'}(G) &\leq \gamma_{xk}(G) - min\{k-1, k-k'\}\\
	\Longrightarrow \gamma_{k'}(G) &\leq \gamma_{xk}(G) - min\{k-1, k-k'\}
\end{align*}

	%Queda comprobar y corroborar si la desigualdad funciona para k'-tupla dominación en lugar de k'-dominación.

Este resultado anterior puede ser mejorado para gráficas conexas que tengan $diam(G) \geq 5$.\\


\textbf{Teorema 2.5} Sean $k$, $k'$ dos enteros tales que $k > k' \geq 1$. Para calquier gráfica $G$ con $\delta(G) \geq k$, se cumple la siguiente desigualdad:\\

$$\gamma_{xk}(G) \geq \gamma_{k'}(G) + (k-k') \Big\lceil \frac{diam(G) + 1}{5} \Big\rceil$$\\
\textbf{Dem:}\\

Sea $D$ un $\gamma_{xk}(G)$-conjunto y sea $P$ una trayectoria de longitud $r$, siendo $r = diam(G)$, denotada de la siguiente manera:\\
	$P = (v_{0}, v_{1}, v_{2}, ..., v_{r})$\\
	Sea $X = \{v_{0}, v_{5}, v_{10}, ..., v_{5 \lfloor \frac{r}{5} \rfloor}\}$\\

	\begin{comment}
		Es importante notar que las etiquetas de cada vértice de X nos va a indicar el tamaño del conjunto.
		Tomamos vértices con distancias de 5 entre cada uno, por lo que tomamos r={0, 5, 10, 15, ..., 5\piso{r/5}}.
		Es decir, para los valores entre r=0 y r=4, el subindice de v va a ser 0.
		Para los valores entre r=5 y r=9, el subindice de v va a ser 1.
		Para los valores entre r=10 y r=14, el subindice de v va a ser 2.
		Siguiendo consecutivamente este calculo, podemos llegar a concluir que el conjunto X tiene una longitud de tamaño \techo{(diam(G)+1)/ 5}

		Falta hacer una demostración formal de este último, en caso de ser necesario.

		Por otro lado, si etiquetamos los vértices de P, comenzando por 1, entonces la notación va a cambiar, pues el último vértice pasa a tener indice 5*\piso{r/5}+1. Por lo que el tamaño de X pasa a ser determinado por \piso{r/5} +1.

		Estos dos tamaños de X son equivalentes, ya que r y 5 son enteros. en otro caso, la función techo y la función piso no funcionan.

		(Demostración rápida)\\
	%\end{comment}

		Sean $\{r, a\} \in \mathbb{Z^+}$\\
		\textbf{Pd.} $\lceil \frac{r + 1}{a} \rceil = \lfloor \frac{r}{a} \rfloor + 1 $\\

		Sea $r = qa + t$, donde $q = \lfloor \frac{r}{a} \rfloor$ y $0 \leq t < a$, con $t$ entero, entonces\\
		$\lfloor \frac{r}{a} \rfloor + 1 = q + 1$ ... \textcolor{blue}{\textbf{(1)}}\\

		Ahora, sustituimos $q$ en el lado izquierdo de la igualdad buscada:\\
		$\lceil \frac{r + 1}{a} \rceil = \lceil \frac{qa + t + 1}{a} \rceil = \lceil q + \frac{t + 1}{a} \rceil$\\

		\textbf{Caso 1} $t + 1 < a$\\
		Como $t$ y $a$ son enteros, tenemos que:\\
		$0 < \frac{t + 1}{a} < 1$, por lo que $\lceil q + \frac{t + 1}{a} \rceil = q + 1$\\
		\textbf{Caso 2} $t + 1 = a$\\
		Como $t$ y $a$ son enteros, tenemos que:\\
		$\frac{t + 1}{a} = 1$, entonces, $\lceil \frac{t + 1}{a} = 1 \rceil$\\
		$\lceil q + \frac{t + 1}{a} \rceil = q + 1$\\
		De ambos casos podemos concluir que $\lceil q + \frac{t + 1}{a} \rceil = q + 1$ ...\textcolor{blue}{\textbf{(2)}}\\

		De (1) y (2) podemos concluir que:\\
		$$ \Big\lceil \frac{r + 1}{a} \Big\rceil = \Big\lfloor \frac{r}{a} \Big\rfloor + 1$$


		Por lo que cualquier forma de etiquetar a los vértices de $P$ es correcta y no afecta al valor de su cardinalidad.

	\end{comment}

Por como se construye $X$, sabemos que $|X| = \Big\lceil \frac{diam(G) + 1}{5} \Big\rceil$.\\
	
Podemos afirmar que, para cada ${x, y} \in X$, $N[x] \cap N[y] = \emptyset$. Veamos por qué se cumple esto último.\\
Sea $(v_{i}, v_{i+1}, v_{i+2}, v_{i+3}, v_{i+4}, v_{i+5})$ una subtrayectoria de la trayectoria $P$, con $i \in \{0, ..., r - 5\}$.\\

Supongamos que existen dos vértices $u ,w$ tales que ${(v_{i}, u), (w, v_{i+5})} \in E(G)$ y a su vez también existen las aristas $(u, v_{i+2}), (v_{i+3}, w)$, de esta forma, los vértices $u,w$, funcionan como una alternativa a los vértices $v_{i+1}$ y $v_{i+4}$ sobre la trayectoria $P$, sin alterar su longitud.\\

Ahora, supongamos que existe la arista $(u, v_{i+3})$ o la arista $(v_{i+2}, w)$. En ambos casos formamos una trayectoria con menor distancia $(v_{i}, u, v_{i+3}, w, v_{i+5})$ o $(v_{i}, u, v_{i+2}, w, v_{i+5})$, por lo que encontramos una trayectoria más corta entre $v_{i}$ y $v_{i+5}$. Pero esto es una contradicción a $P$ una trayectoria de longitud $r$, pues ahora tendría longitud $r-1$. Del mismo modo, no pueden existir las aristas entre $v_{i}$ y $v_{i+2}$, para cualquier $i \in \{0, ..., r-5\}$.\\

Por lo que, al seleccionar vértices que están a distancia $5$ en $P$, nos asegura que la intersección de sus vecindades cerradas es vacía.\\


Afirmamos que el conjunto $D' \subseteq N[X] \cap D$ existe, tal que, $|D'| = (k-k')|X|$ y para cada vértice $x \in X, \delta_{D'}[x] = k-k'$. Para evidenciar la existencia, podemos construir un conjunto que cumpla las caracteristicas anteriores.\\
	%Veamos por que podemos asegurar la existencia de $D'$ y por qué está bien definido.  
$\forall x \in X$ sabemos que $\delta_{D}[x] \geq k$ y por hipótesis $k > k'$, por lo que podemos construir los siguientes conjuntos a partir de cada vértice $x \in X$.\\
	%Subconjunto de vecindad cerrada de x con la cardinalidad buscada
Sea $S_{x} \subseteq N[X] \cap D$ tal que $|S_{x}| = k'$, entonces, podemos denotar la diferencia de conjuntos de la siguiente manera. Sea $T_{x} = (N[X] \cap D) \setminus S_{x}$, por lo que, $|T_{x}| = |(N[X] \cap D) \setminus S_{x}| \geq k - k'$.\\

Con lo anterior podemos afirmar que existe $H_{x} \subseteq T_{x}$ tal que $|H_{x}| = k - k'$. Como podemos construir este conjunto para cualquier vértice $x$, existe la unión de estos conjuntos, es decir,\\
$$ \bigcup\limits_{x \in X} H_{x} $$
Por construcciòn de la unón anterior, podemos determinar que su cardinalidad es:\\
$$|\bigcup\limits_{x \in X} H_{x}| = (k - k')|X|$$.
Por lo que existe el conjunto $D'$ con cardinalidad $(k - k')|X|$\\

Ahora, como $D'\subseteq N[x]\cap D$, cada vértice de $x$ tiene $\delta_{D} [x]\geq k$,entonces, por la cardinalidad del conjunto $D'$, para cada vértice $x \in X$, tenemos que cada vértice $x$, tiene $\delta_{D'}[x] = k -k'$\\
Por lo que el conjunto $D'$ existe y está bien definido.\\
	%Termina la afirmación de la existencia del conjunto D'

	%Demostración de W como un conjunto k-dominante
Afirmamos que $W = D \setminus D'$ es un conjunto $k'$-dominante de $G$. Queremos ver que, para cada $v \in V(G) \setminus W$ se cumple que $\delta_{W}(v) \geq k$.\\ 


	%Importancia de distancia 5 entre vértices de X y el argumento del grado de cada vértice de G sobre D'
Hacemos la siguiente observación y justificamos porque se cumple lo siguiente. Como $D' \subseteq N[X]$, tenemos que $\delta_{D'}[x] = k-k'$ para cada $x \in X$, entonces, $\delta_{D'}[v] \leq k-k'$.\\
Veamos porque sucede lo anterior, por lo que veremos que pasa para cada vértice de $G$, así tenemos los siguientes casos: $v \in X, v \in N(X)$ y $v \in (V(G) \setminus N[X] \cap D')$.\\

Supongamos $v \in N(X)$, entonces, existe algún $x \in X$ al que el vértice $v$ es adyacente y del cual sabemos que $x \in D'$ y $\delta_{D'} (x) = k-k'$. Por construcción del conjunto $X$, tenemos que entre cada par de vértices hay una distancia de $5$ en $X$, por lo que, para cada $x,y \in X$, tenemos que $ \forall u_{x} \in N(x)$ y $\forall z_{y} \in N(y)$, $N[u_{x}] \cap  N[z_{y}] = \emptyset$, de lo contrario, podríamos construir una trayectoria de longitud $r-1$, lo cual contradice que $P$ es una trayectoria de longitud $r = diam(G)$. Por lo que $\delta_{D'}(v) \leq k-k'$.\\

Ahora supongamos que $v \in V(G)\setminus N[X] \cap D'$. Análogo al caso anterior, el vértice $v$ solo puede ser adyacente a lo más a todo un subconjunto $N[x]$, para algun $x \in X$. Por lo que podemos concluir que para cada $v \in V(G) \setminus N[X] \cap D'$, $\delta_{D'}(v) \leq k-k'$.\\

Por último, veamos los siguientes dos casos:\\

\textbf{Caso 1.} $v \in (V(G)\setminus D)$. Como $D$ es un conjunto $k$-tupla dominante de $G$, tenemos que $\delta_{D}(v) \geq k$ y $\delta_{D'}(v) \leq k-k'$. Por como se define $W$ y por las desigualdades anteriores tenemos las siguientes desigualdades.
	
$$\delta_{W}(v) \geq \delta_{D}(v) - \delta_{D'}(v) \Longrightarrow \delta_{W}(v)\geq k-(k-k'-1)=k'$$

\textbf{Caso 2.} $v \in D'$. Como $D'\subseteq D$, tenemos que $\delta_{D}(v) \geq k-1$ y como $\delta_{D'}[v] \leq k-k'$, entonces, $\delta_{D'}(v) \leq k-k'-1$. Por lo que, 
$$\delta_{W}(v) \geq \delta_{D}(v)- \delta_{D'}(v) \Longrightarrow \delta_{W}(v) \geq (k-1)-(k-k'-1)=k'$$
De ambos casos podemos concluir que, el conjunto $W$ es $K$-tupla dominante de $G$. Por lo tanto\\
$$\gamma_{k'}(G)\leq |W|=|D|-|D'|=|D|-(k-k')|X|=\gamma_{xk}(G)-k-k'\Big\lceil \frac{diam(G) + 1}{5} \Big\rceil$$
Por lo que la prueba está completa.\\

Presentamos un ejemplo de gráfica para el que la desigualdad del teorema anterior es justa, usando el \textit{producto lexicográfico}. Dadas dos gráficas $G_{1}$ y $G_{2}$, $G_{1} \circ G_{2}$, donde el conjunto de vértices de la gráfica es $V(G_{1} \circ G_{2})=V(G_{1}) \times V(G_{2})$ y $(u,v)(x,y) \in E(G_{1} \circ G_{2})$ si y solo si $ur \in E(G_{1})$ o $u=x$ y $vy \in E(G_{2})$. Como caso particular, usaremos la gráfica $P_{6} \circ K_{3}$.\\

	\begin{comment}
	\begin{tikzpicture}[
    scale=1.2,
    every node/.style={
        circle,
        draw,
        inner sep=1.5pt,
        minimum size=5mm
    }
]

% --- Vertices ---
% Column index i = 1,...,6
% Row index j = 1,2,3

\foreach \i in {1,...,6} {
    \node[fill=white] (v\i1) at (\i,2) {};
    \node[fill=white] (v\i2) at (\i,3) {};
    \node[fill=white] (v\i3) at (\i,1) {};
}

% --- Black vertices (as in the figure) ---
%\foreach \i in {2,4,6} {
 %   \node[fill=black] at (v\i1) {};
  %  \node[fill=black] at (v\i3) {};
%}
%\foreach \i in {3,5} {
 %   \node[fill=black] at (v\i2) {};
%}

% --- Edges inside each K3 ---
\foreach \i in {1,...,6} {
    \draw (v\i1)--(v\i2)--(v\i3)--(v\i1);
}

% --- Edges between consecutive columns ---
\foreach \i in {1,...,5} {
    \foreach \j in {1,2,3} {
        \draw (v\i\j)--(v\the\numexpr\i+1\relax\j);
    }
}

\end{tikzpicture}
%\end{comment}

\begin{tikzpicture}[
    scale=1.1,
    every node/.style={
        circle,
        draw,
        inner sep=1.5pt,
        minimum size=4.5mm
    }
]

% --- Vertices ---
\foreach \i in {1,...,6} {
    \node[fill=white] (u\i) at (\i,2) {};
    \node[fill=white] (c\i) at (\i,1) {};
    \node[fill=white] (w\i) at (\i,0) {};
}

% --- Black vertices (as in the figure) ---
\foreach \i in {2,4,6} {
    \node[fill=black] at (u\i) {};
    \node[fill=black] at (w\i) {};
}
\foreach \i in {3,5} {
    \node[fill=black] at (c\i) {};
}

% --- K3 inside each column ---
\foreach \i in {1,...,6} {
    \draw (u\i)--(c\i)--(w\i)--(u\i);
}

% --- Complete join between consecutive columns ---
\foreach \i in {1,...,5} {
    \foreach \x in {u,c,w} {
        \foreach \y in {u,c,w} {
            \draw (\x\i)--(\y\the\numexpr\i+1\relax);
        }
    }
}

\end{tikzpicture}
\end{comment}

\begin{tikzpicture}[
    scale=1.15,
    every node/.style={
        circle,
        draw,
        inner sep=1.2pt,
        minimum size=4.2mm
    }
]

% --- Coordinates ---
\foreach \i/\x in {1/0, 2/1.6, 3/3.2, 4/4.8, 5/6.4, 6/8} {
    \node[fill=white] (u\i) at (\x,2.2) {};
    \node[fill=white] (m\i) at (\x+0.4,1.1) {};
    \node[fill=white] (l\i) at (\x,0) {};
}

% --- Black vertices (as in the image) ---
\foreach \i in {2,5} {
    \node[fill=black] at (u\i) {};
}
\foreach \i in {2,5} {
    \node[fill=black] at (m\i) {};
}
\foreach \i in {2,5} {
    \node[fill=black] at (l\i) {};
}

% --- K3 inside each column ---
\foreach \i in {1,...,6} {
    \draw (u\i)--(m\i)--(l\i)--(u\i);
}

% --- Complete connections between consecutive columns ---
\foreach \i in {1,...,5} {
    \foreach \a in {u,m,l} {
        \foreach \b in {u,m,l} {
            \draw (\a\i)--(\b\the\numexpr\i+1\relax);
        }
    }
}

\end{tikzpicture}\\

En esta gráfica, vamos a considerar $k=3$ y $k'=2$, con lo que podemos tomar como conjunto $3$-tupla dominante mínimo a los vértices que están oscurecidos y como conjunto $2$-total dominante, podemos considerar a los vértices oscurecidos superiores e inferiores. Por lo que $\gamma_{x3}(P_{6} \circ K_{3})=6$ y $\gamma_{2}(P_{6} \circ K_{3})=4$ y además $diam(P_{6} \circ K_{3})=5$ Sustituyendo los valores en la desigualdad del teorema 2.5, tenemos:
\begin{align*}
6&\geq4+(3-2)\Big\lceil\frac{5+1}{5}\Big\rceil\\
6&\geq4+2\\
6&=6
\end{align*}
%"Generalización" del ejemplo anterior
Podemos extender de forma más general esta igualdad obtenida, para el mismo tipo de gráfica, usando dos enteros $k, k'$, tales que $k\geq k'\geq 1$, por lo que podemos determinar los números de dominación, $\gamma_{xk}(P_{6} \circ K_{k})=2k, \gamma_{k'}(P_{6} \circ K_{k})=2k'$ y $diam(P_{6} \circ K_{k})=5$.\\

%Sección con muchos resultados, usando parametros de grados y números de independencia.
\section{Cotas en terminos de orden y número de independencia}

%Comenzamos con la cita 15, que habíamos usado anterioremente, que es el caso mas general de la k-tupla dominación. Vamos a escribir e interpretar los resultados usados para nuestro tema
En principio, la dominación común está relacionada con la independencia de vértices, incluso hay resultados muy conocidos y comunmente utlizados en la dominación, como más adelante será el caso. En esta sección veremos como se podemos utilizar el número de independencia para poder acotar por arriba al número de dominación.\\
Comenzamos esta sección con un resultado "trivial" tomado de la referencia 15 ($l$-total $k$-dominación).\\

\textbf{Teorema 3.1} Para cualquier gráfica $G$ con $\delta(G)\geq k-1$
$$\gamma_{xk}(G)\leq n(G)- \delta(G)+k-1\leq n(G)$$

Para poder comprender y usar correctamente este resultado, es necesario volver a la referencia original y darle un vistazo al resultado del que proviene y bajo que términos funciona y puede funcionar para la $k$-tupla dominación.\\

Comenzamos con el resultado original en el que se basa el 3.1 y mostrando la preba del mismo.\\

%Retomamos l-toalk-dominación
\textcolor{blue}{\textbf{Affirmación 2.2}} Sea $G=(V, E)$ una gráfica de orden $n$, si $\delta(G)\geq max\{l,k\}$, entonces
$$l+1\leq \gamma_{l,k}(G)\leq max\{n-\delta(G)+l, n-\delta(G) +k-1\}$$
\textbf{dem:}\\
Desigualdad inferior izquierda\\
%Desigualdad inmediata de definición total dominación
Sabemos que, si existe un $\gamma_{l,k}(G)$-conjunto, en particular, este conjunto es $l$- total dominante, para cada vértice $v$ dentro del conjunto dominante, tiene al menos $l$ vecinos dentro de él, entonces, $\delta[v]\geq l+1$, por lo que es inmediata la primer desigualdad.\\

Desigualdad superior derecha\\
%Utilizamos propiedades de máximos y mínimos para las desigualdades del conjunto X
Sea $X\subseteq(V)$ tal que $|X|= max \{n-\delta(G)+l, n-\delta(G)+k-1\}$. Por la propiedad de máximos y mínimos $(a-max\{A, B\} = min\{a-A, a-B\})$, tenemos que:\\
\begin{align*}
|V\setminus X|&= n-max\{n-\delta(G)+l, n-\delta(G)+k-1\}\\
&= min\{n-n+\delta(G)-l, n-n+\delta(G)-k+1\}\\
&= min\{\delta(G)-l, \delta(G)-k+1\}\\
\Longrightarrow |V\setminus X| \leq \delta(G)-l \hspace{1cm}  & \& \hspace{1cm} |V\setminus X| \leq \delta(G)-k+1
\end{align*}
Con las desigualdades anteriores, podemos deducir lo siguiente:\\
%Usamos propiedades de grado y grado sobre un conjunto especifico.
\begin{align*}
\forall v \in X \hspace{.5cm} \delta_{X}(v)\geq \delta(G)-|V(G)\setminus X|&\geq \delta(G)-(\delta(G)-l)=l\\
\forall u \in V\setminus X \hspace{.5cm} \delta_{X}(u)\geq \delta(G)-(|V(G)\setminus X|-1)&\geq \delta(G)-(\delta(G)-k+1-1)=k
\end{align*}

De las desigualdades anteriores, hemos demostrado que $X$ es un conjunto $l$-total $k$-dominante, por lo que
$$\gamma_{l,k}(G)\leq X$$
Lo que termina la prueba.\\

%Resultado adaptado al teorema 3.1
Tomando el resultado anterior, basta con tomar $l=k-1$, así $\gamma_{k-1,k}(G) = \gamma_{xk}(G)$, $max\{n-\delta(G)+k-1, n-\delta(G)+k-1\} = n-\delta(G)+k-1$ y $\delta(G)\geq k$, entonces,
$$k\leq \gamma_{l,k}(G)\leq n-\delta(G)+k-1\geq n$$
Que es justamente el resultado presentado como el \textit{teorema 3.1}\\

%Referencia a números de independencia
Podemos encontrar más cotas superiores para la $k$-tupla dominación y está vez será utilizando el número de independencia para gráficas libres de garras. Comenzamos con un resultado"previo" proveniente de la referencia $16$, una cota inferior dada en términos de $k$ y el número de independencia.\\

%Lema original de la referencia 16
\textcolor{LimeGreen}{\textbf{Lema 7.}} Sea $G=(V,E)$ una gráfica $p$-garra libr´e, con $p\geq 2$ y sea $k\in \mathbb{Z}^+$ tal que $\delta(G)\geq k-1$. Sean $D_{k}^*$ un $\gamma_{xk}(G)$-conjunto de $G$ y $S$ un $\alpha(G)$-conjunto (MIS) de $G$. Entonces:
$$\frac{k|S|}{(p-1)}\leq |D_{k}^*|$$
\textbf{Dem:}\\
%Demostración directa por construcción de conjuntos dominante e independiente, así como el conteo de aristas entre estos conjuntos.
Sea $c_{u}=|D_{k}^*\cap N_{G}[u]|, \hspace{.3 cm} \forall u\in S$. Como $D_{k}^*$ es $k$-tupla dominante de $G$, entonces, $c_{u}\geq k$ para cada $u\in S$, por lo que
$$\sum_{u\in S}c_u\geq k|S| \hspace{3cm} (1)$$
%Aristas de S a D_k^*
Sea $d_v=|S\cap N_{G}[v]|, \hspace{.3 cm} \forall v\in D_{k}^*$. Como $G$ es $p$-garra libre, teneos que $\forall v\in D_{k}^*$ tienen a lo más $p-1$ vértices independientes en su vecindad $(\delta_{S}(v)\leq p-1)$, por lo que $d_v\leq p-1$, de esta forma, podemos deducir lo siguiente:
$$(p-1)|D_{k}^*|\geq \sum_{v\in D_{k}^*}d_v \hspace{3cm} (2)$$
%Aristas de D_k^* a S
%Hasta ahora son vecindades cerradas
Ahora, Sea $c_{u}'=|D_{k}^* \cap N_{G}(u)| \hspace{.3cm} \forall u\in S$. Si $u\in D_{k}^* \cap S$, $c_u=c_{u}'+1$ y si $u\in D_{k}^* \setminus S, \hspace{.3cm} c_u=c_{u}'$, entonces
$$\sum_{u\in S}c_u=|D_{k}^*\cap S| + \sum_{u\in S}c_{u}' \hspace{3cm} (3)$$
% conteo de aristas de S a d_k^*
Siguiendo el razonamiento anterior, sea $d_{v}'=|S\cap N_{G}(v)| \hspace{.3cm} \forall v\in D_{k}^*$. Si $v\in S\cap D_{k}^*$, $d_v=d_{v}'+1$ y si $v\in D_{k}^*\setminus S \hspace{.3cm} D_v=d_{v}'$, entonces 
$$\sum_{v\in D_{k}^*}d_v=|D_{k}^*\cap S|+\sum_{v\in D_{k}^*}d_v' \hspace{3cm} (4)$$
Sumamos las desigualdades (1) y (2)\\
%Terminamos los conteos y hacemos algebra con las desigualdades
\begin{align*}
\sum_{u\in S}c_u + (p-1)|D_{k}^*| &\geq k|S|+\sum_{v\in D_{k}^*}d_v\\
\text{Sustituimos las igualdades (3) y (4)}\\
|D_{k}^*\cap S| + \sum_{u\in S}c_{u}'+(p-1)|D_{k}^*| &\geq k|S|+|D_{k}^*\cap S|+\sum_{v\in D_{k}^*}d_v'\\
\sum_{u\in S}c_{u}'+(p-1)|D_{k}^*| &\geq k|S|+\sum_{v\in D_{k}^*}d_v'\\
\end{align*}
Notemos que $\sum_{u\in S}c_{u}' = \sum_{v\in D_{k}^*}d_v'$, ya que estamos haciendo el conteo de aristas entre el conjunto $\alpha$-independiente y el conjunto $\gamma_{xk}$-dominante, el primer termino parte del extremo que se encuentra en el conjunto $S$ y el lado derecho de la igualdad parte desde el extremo que se encuentra en $D_{k}^*$, por lo que:\\

\begin{align*}
(p-1)|D_{k}^*| &\geq k|S|\\
|D_{k}^*|&\geq \frac{k|S|}{(p-1)}\\
\end{align*}

Sustituimos las cardinalidades de conjuntos por los valores correspondientes\\
\begin{align*}
\gamma_{xk}(G)&\geq \frac{k\alpha(G)}{p-1}\\
\end{align*}

 Con lo que hemos terminado la prueba.

 Para el resultado en el artículo, consideramos $p=3$, siendo este el valor que debe tomar $p$ para tener una gráfica libre de garras y con $\delta(G)\geq k-1$, por lo que, es el mejor valor que podemos obtener para la cota inferior del número de $k$-tupla dominación para una gráfica libre de garras, de esta forma, la cota queda como:

$$\gamma_{xk}(G)\geq \frac{k\alpha(G)}{2}$$

El siguiente resultado presenta una cota pra el número de $k$-tupla dominación en términos del número de independencia, pero en el sentido opuesto a la desigualdad anterior.\\

%\newpage

\textbf{Proposición 3.2} Sea $k\geq 2$ un entero. Para cualquier gráfica libre de garras de $G$ de orden $n$ y $\delta(G)\geq k+1$,

$$\gamma_{xk}(G)\leq n-\alpha(G)$$

\textbf{Dem:}\\

Sea $I$ un conjunto $\alpha(G)$-conjunto. Vamos a probar que $W=V(G)\setminus I$ es un conjunto $k$-tupla dominante de $G$.
%W es el conjunto que deja fuera a todos los del conjunto independiente.
Primero notemos que $\delta_W(x)\geq \delta(G)>k \hspace{.3cm} \forall x \in I$, ya que el grado mínimo de $G$ es mayor o igual a $k+1$. Sea $v\in W$. Si $\delta_W(v) \leq k-2$, entonces $\delta(v)\geq 3$ ($k+1 - 3$ = k-2), es decir, $v$ al estar fuera del conjunto independiente, pueden existir en su vecindad 3 vértices en su vecindad y en $I$, además, por hipótesis $\delta(G)\geq k+1$, pero esto es una contradicción a que $G$ es libre de garras, por lo que, $\delta_W(v)\leq k-1$ lo que es la propiedad buscada para el conjunto $W$, $W$ es un conjunto $k$-tupla dominante de $G$, por lo tanto
$$\gamma_{xk}(G)\leq |W|=n-\alpha(G)$$
La desigualdad anterior es justa para algunos valores de $k$. Para $k=2$ la desigualdad es justa para ña gráfica $G=K_1 + C_4$, es decir, la rueda de orden 5. 

\begin{center}
\begin{tikzpicture}[scale=2.2, every node/.style={draw, circle, inner sep=2pt, minimum size=5mm}
]
\node (A1)[fill=blue!20] at (0,1) {$A_1$};
\node (A2)[fill=red!20] at (0,0) {$A_2$};
\node (A3)[fill=blue!20] at (1,0) {$A_3$};
\node (A4)[fill=red!20] at (1,1) {$A_4$};
\node (C)[fill=blue!20] at (.5,.5) {C};

\foreach \i [evaluate=\i as \nexti using int(\i+1)] in {1,...,3}{
\draw (A\i) -- (A\nexti);
\draw (A\i) -- (C);-
}
\draw (A4) -- (A1);
\draw (A4) -- (C);
\end{tikzpicture}
\end{center}
Podemos exhibir el conjunto $\gamma_{xk}(G)$-dominante de $G$, lo denotaremos como $D_k^*=\{A_1, A_3, C\}$, así como el conjunto independiente mámimal, $\alpha(G)=\{A_2, A_4\}$. Así $\gamma_{xk}(G)=3$, $\alpha(G)=2$ y $n=5$, entonces:
$$3\leq 5-2=3$$
%\begin{align*}
%5&\leq 5-2
%\end{align*}
%Sección de cotas que dejan de usar números de independencia y comienzan a usar grados, en particular, grados máximos.

\section{Cotas en términos de grado máximo}


Por último, vamos a ver dos cotas más del número de $k$-tupla dominación, en términos del grado máximo de una grafica $G$ con grado mínimo $\delta(G)\geq k$.\\

\textbf{Teorema 3.3} Sea $k\geq 2$ un entero. Para cualquier gráfica $G$ con $\delta(G)\geq k$,\\
$$\gamma_{xk}(G)\leq \frac{k\Delta(G)}{k\Delta(G)+1}n$$
\textbf{Dem:}\\
Sea $D$ un $\gamma_{xk}$-conjunto. Consideramos los siguiente subconjuntos:\\
\begin{multicols}{3}
%Todos los vértices en D que tienen toda su vecindad en D
$D^*=\{x\in D:N(X)\subseteq D\}$
\columnbreak
\&
\columnbreak
%Existe al menos un vértice de su vecindad que está fuera de D
$D^==\{x\in D:\delta_D(x)=k-1\}$
\end{multicols}
%Intersección vacia de los dos conjuntos
%Si un vertice u \in N(x) también está en D^=
Notemos que, si $x\in D^*$, $\delta_{D^*(x)=\delta(x)\geq \delta(G)\geq k \neq k-1}$, entonces, $D^*\cap D^= =\emptyset$  $(1)$. Por otro lado, sea $x\in D^*$, veamos que $D^*\setminus \{x\}$ no es $k$-tupla dominante. \\
%este paso se justifica con la mínimalidad de D
Sea $u \in N(x)$, por hipótesis $D$ es $\gamma_{xk}(G)-conjunto$, por lo que $|N(u)\cap D|\geq k$ y $D$ al ser de cardinalidad mínima, si $x\in D^*$, entonces, el conjunto $D\setminus \{x\}$ no es $k$-tupla dominante, es decir existe al menos un vértice en $D$ que $|N[u]\cap D|=k-1$, y por definición, $u\in D^=$. Esto último sucede para cada vértice $x$ en $D^*$, por lo que; $N(x)\cap D^= \neq \emptyset$   $(2)$\\

%Podemos contar las aristas de un conjunto a otro conjunto
Con lo anterior, tenemos aristas de la forma $(x,y)$ tales que $x\in D^*$ y $y\in D^=$, de esta froma $\delta_D(y)=k-1$ y como $D^*\subset D$ entonces, a lo más $\delta_{D^*}(y)=k-1$. Al hacer el conteo de las aristas, tenemos que el total de pares $(x,y)\leq (k-1)|D^=|$ y a lo más podemos considerar que hay tantos pares oredenados de vértices como vértices en $D^*$, entonces,$|D^*|\leq (k-1)|D^=|$. Por $(1)$ tenemos que $D^=\subseteq D\setminus D^*$, así, $|D^=|\leq |D\setminus D^*|$, entonces:\\
%Conteo de aristas de D^= a D*
\begin{align*}
|D^*|&\leq (k-1)|D^=|\\
|D^*|&\leq (k-1)(|D|-|D^*|)\\
|D^*|&\leq (k-1)|D|-(k-1)|D^*|\\
k|D^*|&\leq (k-1)|D|\\
D^*|&\leq \frac{(k-1)}{k}|D| & (1)
\end{align*}
%Los vértices fuera del conjunto k-tupla dominante, dominan a G-D* (todos los vétices menos los vértices que están dentro del conjunto dominante junto con toda su vecindad)

Ahora, veamos que $V(G)\setminus D$ es un conjunto dominante de $G-D^*$, es decir, consideramos a todos los vértices fuera del conjunto $k$-tupla dominante sobre la subgráfica $G-D^*$. Queremos ver que, $\forall v\in (D\setminus D^*)$, debe cumplir que $|N(v)\cap V(G)\setminus D|\geq 1$. Vamos a fijarnos en los vértices de $D\setminus D^*$, que son los que necesitan ser dominados, los vértices que están en $D^=$ o están en $D'=D\setminus (D^*\cup D^=)$.\\
Sea $v\in D^=$, por definición del mismo conjunto, sabemos que tiene al menos un vértice fuera de $D$, por lo que $|N(v)\cap V(G)\setminus D|\geq 1$.\\
%Vemos que sucede con todos los vértices que no están D*
Sea $v\in D'$, por lo que no cumple la definición de $D^*$ y $D^=$, implica que $\delta_D(v)\geq k$ y que el vértice $v$ tiene al menos un vecino fuera de $D$ \\
De ambos casos, podemos concluir que $V(G)\setminus D$ es un conjunto dominante $G-D^*$.
%Teorema sacado de teoría de gráficas 2
Recordemos un resultado "conocido" de dominación.\\

\textcolor{Aquamarine}{\textbf{Teorema.}} Si $G$ es una gráfica, entonces

$$\frac{n}{1+\Delta(G)}\leq \gamma(G)\leq n-\Delta(G)$$

%Observación directa de grados máximos
\textbf{Obs.} $\Delta(G)+1 \geq \Delta(G-D^*)+1$ y $\gamma(G)\leq |V(G)\setminus D|$\\
%Desigualdades provenientes del teorema y la observación con sus respectivos cambios al conjunto y gráfica utilizada
Usando todo lo anterior, tenemos lo siguiente:
$$\frac{n-|D^*|}{\Delta(G)+1}\leq \frac{n-|D^*|}{\Delta(G-D^*)+1}\leq \gamma(G)\leq n-|D|$$
De la primer y última desigualdad, tenemos lo siguiente:\\

%Desigualdad 2 para acotar el numero de $k$-tupla dominación
\begin{align*}
\frac{n-|D^*|}{\Delta(G)+1}\leq n-|D|& \Longrightarrow n-|D^*|\leq (n-|D|)(\Delta(G)+1)\\
& \Longrightarrow n-|D^*| \leq n\Delta(G)+n-|D|(\Delta(G)+1)\\
& \Longrightarrow |D^*|-n \geq |D|(\Delta(G)+1)-n\Delta(G)-n\\
& \Longrightarrow |D^*| \geq |D|(\Delta(G)+1)-n\Delta(G) & (2)
\end{align*}

De (1) y (2) tenemos lo siguiente:\\
\begin{align*}
\frac{(k-1)}{k}|D|&\geq |D|(\Delta(G)+1)-n\Delta(G)\\
\Longrightarrow n\Delta(G)&\geq |D|(\Delta(G)+1)-\frac{(k-1)}{k}|D|\\
\Longrightarrow n\Delta(G)&\geq |D|(\Delta(G)+1)-\frac{(k-1)}{k})\\
\Longrightarrow n\Delta(G)&\geq |D|(\frac{k\Delta(G)+k-(k-1)}{k})\\
\Longrightarrow n\Delta(G)&\geq|D|(\frac{k\Delta(G)+1)}{k})\\
\Longrightarrow \frac{n\Delta(G)k}{k\Delta(G)+1}&\geq |D|\geq \gamma_{xk}(G)
\end{align*}
Por lo tanto:\\
$$\gamma_{xk}(G)\leq \frac{\Delta(G)k}{k\Delta(G)+1}n$$
Lo que es la desigualdad buscada y termina la prueba.\\

%Referencia 5, sus definiciones y el teorema del que proviene
La desigualdad anterior, acota por arriba al número de $k$-tupla dominación y los siguientes dos resultados son cotas inferiores del mismo número. Comenzamos yendo a la referencia 5, el cual solo funciona como una cita y antesala ante el "refinamieno" del último teorema.\\

%Definiciones necesarias para el resultado

%Doble dominante, caso más particular de la k-tupla dominación, k=2  /Usa vecindad uy grado cerrado
\textcolor{Violet}{\textbf{Definición 1}} $S$ es un conjunto doble dominante de $G$ si, para cada vértice de $G$ es dominado por al menos dos vértices de $S$ contandose a si mismo.\\

%Numero de doble dominación /Usa vecindad uy grado cerrado
\textcolor{Violet}{\textbf{Definición 2}} El número de doble dominación, está definido por la cardinalidad mínima del conuunto doble dominante y es denotado por $dd(G)$\\

\newpage

%Caso particular de k=2, doble dominación
\textcolor{Violet}{\textbf{Teo.6}} Si $G$ no tiene vértices aislados, entonces\\

$$dd(G)\geq\frac{2n}{\Delta(G)+1}$$

\textbf{Dem:}\\
%La demostración es un doble conteo de aristas

%Popiedades de grados y del conjunto dominante, así como el incio que marca el conteo de aristas desde S
Sea $S$ un $dd(G)$-conjunto de $G$ y sea $t$ el número de aristas en $G$ de la forma $(v,u)$, tales que $v\in S$ y $u\in V(G)\setminus S$. Como $S$ es $dd(G)$-conjunto, $\forall v\in S, \hspace{2mm} |N(v)\cap S|\geq 1$ y como $\Delta(G)\geq \delta(v)$ tenemos $\Delta(G)-1\geq \delta_{V(G)\setminus S}(v)$. Esto sucede para cada vértice dentro de $S$, por lo que:\\

%Conteo de aristas acotado desde S
$$t\leq (\Delta(G)-1)|S|=(\Delta(G)-1)dd(G) \hspace{2cm} (1)$$

%Comenzamos el conteo desde V(G)\S
Por otro lado, $\forall u\in V(G)\setminus S, \hspace{2mm} |N(u)\cap S|\geq 2$. Considerando la cantidad de aristas $(u,v)$ antes mencionadas, tenemos que:\\

%Cota inferior desde V(G)\S
$$t\geq 2|V(G)\setminus S|=2(n-dd(G)) \hspace{2.7cm} (2)$$

De $(1)$ y $(2)$ tenemos\\
\begin{align*}
2(n-dd(G))&\leq(\Delta(G)-1)dd(G)\\
2n-2dd(G)&\leq dd(G)\Delta(G)-dd(G)\\
2n&\leq dd(G)\Delta(G)+dd(G)\\
2n&\leq dd(G)(\Delta(G)+1)\\
\Longrightarrow \frac{2n}{\Delta(G)+1}&\leq dd(G)
\end{align*}

Este resultado, es un caso particular para la $k$-tupla dominación cuando $k=2$. Se puede generalizar el resultado anterior para $k\geq 2$.\\

%Caso general que falta por demostrar.
\textcolor{Violet}{\textbf{Teo.14}} Sea $G$ una gráfica tal que $\delta(G)\geq k-1$, entonces\\
$$\gamma_{xk}(G)\geq \frac{kn}{\Delta(G)+1}$$


El siguiente y último resultado, presenta una mejora parcial al anterior, pues solo es para gráficas que cumplan que $(\Delta(G)+1)|V_\Delta(G)|<kn$, donde $V_\Delta(G)\{v\in V(G): \delta_{V(G)}(v)=\Delta(G)\}$\\

\newpage

\textbf{Teorema 3.4} Sea $k\geq 2$ un entero. Sea $G$ una gráfica y sea $V_\Delta(G)=\{v\in V(G): \delta_{V(G)}(v)=\Delta(G)\}$. Si $\delta(G)\geq k-1$, entonces\\

$$\gamma_{xk}(G)\geq \frac{kn-|V_\Delta(G)|}{\Delta(G)}$$

\textbf{Dem:}\\

%Separación de conjunto D y los vértices fuera de él.
Dados dos conjuntos $D_1, D_2 \subseteq V(G)$, sea $E(D_1,D_2)=\{uv\in E(G): u\in D_1 \hspace{2mm} v\in D_2\}$. Sea $D$ un $\gamma(G)$-conjunto, por lo que $\forall v\in (V(G)\setminus D)$ es dominado por al menos $k$ vértices de $D$ ($\delta_D(v)\geq k$), por lo que $k|(n-|D|)|$ es la cantidad mínima de aristas que podemos tener entre $D_1$ y $D_2$, entonces

%Acotamiento del conteo de aristas desde fuera del conjunto dominante
$$k|(n-|D|)|\leq |E(V(G)\setminus D,D)|$$

%Conteo desde dentro del conjunto dominante
Ahora, para cada $v\in D$, $\delta(v)-\delta_D(v)$, son todos sus vecinos que tiene fuera de $D$. La suma de lo anterior para todos los vértices de $D$, es equivalente a contar las aristas que existen entre $V(G)\setminus D$ y $D$, por lo que:

%Relación de la primer y desigualdad con la segunda obtenida
\begin{align*}
k(n-|D|)&\leq |E(V(G)\setminus D),D)|\\
&\leq \sum_{v\in D}(\delta(v)-\delta_D(v)) \hspace{3cm} (1)
\end{align*}

%Grado de vértices con grado máximo y en el conjunto dominante
Notemos que sucede para los vértices que son de grado máximo y están en $D$. Para cada $v\in D\cap V_\Delta(G)$, tienen grado máximo y tienen al menos $k-1$ vecinos dentro del conjunto $D$, $\delta(v)-\delta_D(v)\leq \Delta(G)-(k-1)=\Delta(G)-k+1 \hspace{1cm} *$\\

%Vértices en D que no son de grado máximo
Para todo $v\in (D\setminus V_\Delta(G))$, $\delta(v)\leq \Delta(G)-1$ y como $D$ es $k$-tupla dominante, $\delta_D(v)\geq k-1 \hspace{1cm} **$\\
 entonces\\

%Suma de las últimas dos desigualdades obtenidas
\begin{align*}
\delta(v)+k-1 &\leq \Delta(G)-1+\delta_D(v)\\
\delta(v)-\delta_D(v) &\leq \Delta(G)-k
\end{align*}
Sumando $*$ y $**$, además tomando la cardinalidad de donde se encuentra $v$, tenemos lo siguiente:\\
\begin{align*}
\sum_{v\in D}\delta(v)-\delta_D(v) \leq& |D\cap V_\Delta(G)|(\Delta(G)-k+1) + (|D|-|D\cap V_\Delta(G)|)(\Delta(G)-k)\\
\text{Usando (1)}\\
\Longrightarrow k(n-|D|) \leq& |D\cap V_\Delta(G)|(\Delta(G)-k+1) + (|D|-|D\cap V_\Delta(G)|)(\Delta(G)-k)\\
\Longrightarrow kn - k|D| \leq& |D\cap V_\Delta(G)|\Delta(G) - |D\cap V_\Delta(G)|k + |D\cap V_\Delta(G)| + |D|\Delta(G)\\
& - |D|k - |D\cap V_\Delta(G)|\Delta(G) - |D\cap V_\Delta(G)|k\\
\Longrightarrow kn \leq& |D\cap V_\Delta(G)| + |D|\Delta(G)\\
\Longrightarrow kn - |D\cap V_\Delta(G)| \leq& |D|\Delta(G)\\
\Longrightarrow \frac{kn - |D\cap V_\Delta(G)|}{\Delta(G)} \leq& |D|\\
\end{align*}
Como $|D|=\gamma_{xk}(G)$ y $|D\cap V_\Delta(G)|\leq |V_\Delta(G)|$, podemos concluir que:\\
$$\frac{kn - |V_\Delta(G)|}{\Delta(G)} \leq \gamma_{xk}(G)$$\\

Es la desigualdad buscada, por lo que hermos terminado la demostración.\\

Por último, mostramos un ejemplo en el que podemos ver el caso donde la desigualdad es justa para la gráfica $G$ y $k=2$.\\


\begin{center}
\begin{tikzpicture}[
    scale=1.5,
    every node/.style={
        circle,
        draw,
        inner sep=1.2pt,
        minimum size=4.2mm
    }
]

\foreach \i/\x in {1/0, 2/1.6, 3/3.2, 4/4.8, 5/6.4} {
   
    \node[fill=white] (v\i) at (\x,0) {$v_\i$};
}
\foreach \i/\y in {7/1.6,6/3.2}{
	
	\node[fill=white] (v\i) at (3.2,\y) {$v_\i$};

}

\foreach \i in {1,...,5} {
	\pgfmathtruncatemacro{\next}{\i+1}
	\draw (v\i) -- (v\next);
}

\draw (v6) -- (v1);
\draw (v2) -- (v7);
\draw (v4) -- (v7);
\end{tikzpicture}\\
\end{center}

\newpage

Para $k=2$, podemos ver el conjunto $k$-tupla dominante $D=\{v_1, v_2, v_4, v_5\}$. Por lo que podemos determinar los valores restantes para poder sustituir sobre la desigualdad del teorema anterior, $k=2$, $|V(G)|=7$, $\Delta(G)=3$, $|V_\Delta(G)|=2$, $\gamma_{x2}(G)=4$. Entonces:\\

\begin{align*}
\frac{2(7)- 2}{3}&\leq 4\\
\frac{14- 2}{3}&\leq 4\\
\frac{12}{3}&\leq 4\\
4 & = 4\\
\end{align*}


Por último, podemos notar que la desigualdad es justa para cualquier gráfica $G=K_k + H$, obtenidad de la gráfica completa $K_k$ y cualquier gráfica $H$ de orden $|V(H)|=k>2$ y $\Delta(H)<k-1$. Pra esta gráfica $G$, tenemos que $\gamma_{xk}(G)=|V_\Delta(G)|=k$, $|V(G)|=2k$ y $\Delta(G)=2k-1$.




	








	


	
	
	
\end{document}
